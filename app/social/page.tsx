import SocialClient from './social-client';

export const metadata = {
  title: 'Social | Ondwira',
  description: 'Private conversations, groups, and the people who matter.',
};

export default function SocialPage() {
  return <SocialClient />;
}
